# Abstract Source Signals

Only abstract, permission-reviewed signals from `PUBLIC_NON_OFFICIAL` sources may be stored here. Never store source question text, answer options, screenshots, close paraphrases, or legal conclusions. Every JSON signal must resolve to a `VERIFIED` source manifest and validate against `schemas/source_signal.schema.json`.

This foundation contains no source signals; all ten development fixtures remain `GEN`.
