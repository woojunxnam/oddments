{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/woojunxnam/oddments/schemas/drug.schema.json",
  "title": "MPJE Drug Record",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "drug_id",
    "content_version",
    "content_hash",
    "generic_name",
    "brand_names",
    "main_indications",
    "therapeutic_class",
    "federal_status",
    "massachusetts_status",
    "legal_consequences",
    "verified_rule_dependencies",
    "authorities",
    "last_verified",
    "verification_status",
    "verification_notes"
  ],
  "properties": {
    "drug_id": {
      "type": "string",
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$"
    },
    "content_version": {"type": "integer", "minimum": 1},
    "content_hash": {"type": "string", "pattern": "^[a-f0-9]{64}$"},
    "generic_name": {"type": "string", "minLength": 2},
    "brand_names": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": {"type": "string", "minLength": 1}
    },
    "main_indications": {
      "type": "array",
      "minItems": 1,
      "items": {"type": "string", "minLength": 3, "maxLength": 180}
    },
    "therapeutic_class": {"type": "string", "minLength": 2},
    "federal_status": {
      "$ref": "#/$defs/federalStatus"
    },
    "massachusetts_status": {
      "$ref": "#/$defs/massachusettsStatus"
    },
    "legal_consequences": {
      "type": "object",
      "additionalProperties": false,
      "required": ["refill", "transfer", "partial_fill", "masspat", "quantity_limit"],
      "properties": {
        "refill": {"$ref": "#/$defs/legalConsequence"},
        "transfer": {"$ref": "#/$defs/legalConsequence"},
        "partial_fill": {"$ref": "#/$defs/legalConsequence"},
        "masspat": {"$ref": "#/$defs/legalConsequence"},
        "quantity_limit": {"$ref": "#/$defs/legalConsequence"}
      }
    },
    "verified_rule_dependencies": {
      "type": "object",
      "patternProperties": {
        "^(MA|FED)-[A-Z0-9]+(?:-[A-Z0-9]+)*$": {"$ref": "#/$defs/dependencySnapshot"}
      },
      "additionalProperties": false
    },
    "authorities": {
      "type": "array",
      "minItems": 1,
      "items": {"$ref": "#/$defs/authority"}
    },
    "last_verified": {"type": "string", "format": "date"},
    "verification_status": {
      "enum": ["HOLD", "PRIMARY_VERIFIED", "OFFICIAL_POLICY_VERIFIED"]
    },
    "verification_notes": {"type": "string"}
  },
  "$defs": {
    "dependencySnapshot": {
      "type": "object",
      "additionalProperties": false,
      "required": ["content_version", "content_hash"],
      "properties": {
        "content_version": {"type": "integer", "minimum": 1},
        "content_hash": {"type": "string", "pattern": "^[a-f0-9]{64}$"}
      }
    },
    "legalConsequence": {
      "type": "object",
      "additionalProperties": false,
      "required": ["summary", "rule_ids"],
      "properties": {
        "summary": {"type": "string", "minLength": 3},
        "rule_ids": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {"type": "string", "pattern": "^(MA|FED)-[A-Z0-9]+(?:-[A-Z0-9]+)*$"}
        }
      }
    },
    "federalStatus": {
      "type": "object",
      "additionalProperties": false,
      "required": ["controlled", "schedule"],
      "properties": {
        "controlled": {"type": "boolean"},
        "schedule": {"enum": ["I", "II", "III", "IV", "V", "NONCONTROLLED"]}
      },
      "allOf": [
        {
          "if": {"properties": {"controlled": {"const": false}}},
          "then": {"properties": {"schedule": {"const": "NONCONTROLLED"}}}
        },
        {
          "if": {"properties": {"controlled": {"const": true}}},
          "then": {"properties": {"schedule": {"enum": ["I", "II", "III", "IV", "V"]}}}
        }
      ]
    },
    "massachusettsStatus": {
      "type": "object",
      "additionalProperties": false,
      "required": ["schedule", "masspat_reportable", "drug_of_concern"],
      "properties": {
        "schedule": {"enum": ["I", "II", "III", "IV", "V", "VI", "NONCONTROLLED"]},
        "masspat_reportable": {"type": "boolean"},
        "drug_of_concern": {"type": "boolean"}
      }
    },
    "authority": {
      "type": "object",
      "additionalProperties": false,
      "required": ["type", "name", "section", "url"],
      "properties": {
        "type": {
          "enum": [
            "FDA_LABEL",
            "FEDERAL_REGULATION",
            "FEDERAL_STATUTE",
            "STATE_REGULATION",
            "BOARD_POLICY",
            "OFFICIAL_GUIDANCE"
          ]
        },
        "name": {"type": "string", "minLength": 2},
        "section": {"type": "string", "minLength": 1},
        "url": {"type": "string", "format": "uri", "pattern": "^https://"}
      }
    }
  }
}
